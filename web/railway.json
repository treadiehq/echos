{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'echo \"🚀 Starting Echos Frontend\" && echo \"PORT=$PORT\" && echo \"Setting NITRO_HOST=0.0.0.0 NITRO_PORT=$PORT\" && export NITRO_HOST=0.0.0.0 && export NITRO_PORT=$PORT && echo \"Checking .output directory:\" && ls -la .output/server/ && echo \"Starting Nitro...\" && node .output/server/index.mjs'",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

