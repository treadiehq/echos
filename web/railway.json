{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && ls -la .output/ && ls -la .output/public/ && ls -la .output/public/_nuxt/"
  },
  "deploy": {
    "startCommand": "HOST=0.0.0.0 npm run start",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "NEVER"
  }
}

